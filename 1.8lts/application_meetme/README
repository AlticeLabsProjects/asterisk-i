Description:

This patch adds changes to the application meetme such as the insertion of commercials and videos on a conference.
The application allows also to mute and unmute any user as well as user management.

 1. Get the source for Asterisk 1.8.3.2 from:
 
 http://downloads.asterisk.org/pub/telephony/asterisk/old-releases/asterisk-1.8.3.2.tar.gz
 
 
 2. Untar the Asterisk 1.8.3.2
 
 $ tar -zxvf asterisk-1.8.3.2.tar.gz
 
 
 3. Add the app WMS to asterisk
 
 $ patch -p1 -d asterisk-1.8.3.2/ < asterisk-i-app-wms.patch
 
 
 4. Add the application meetme
 
 $ patch -p1 -d asterisk-1.8.3.2/ < asterisk-i-app-meetme.patch
 
 
 5. Build Asterisk
 
 $ cd asterisk-1.8.3.2
 $ ./configure
 $ make